<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="comment">
	<insert id="insertComment">
		insert into values(#{comment_no},#{post_no},#{user_id},#{comment_date})
	</insert>

	<delete id="deleteComment">
		delete comment where comment_no=#{comment_no} and user_id=#{user_id} and post_no=#{post_no}
	</delete>

	<update id="updateComment">
		update comment set comment_content=#{comment_content} where post_no=#{post_no} and user_id={#user_id} and comment_no=#{comment_no}
	</update>

	<select id="readComment" resultType="commentVo">
		select * from comment where post_no=#{post_no}
	</select>
</mapper>